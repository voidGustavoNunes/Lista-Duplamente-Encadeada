#include <stdio.h>
#include <stdlib.h>

    int main (void){

    float areaRetang(float base, float altura); //sempre tem que declarar a funcao na main
    float area = areaRetang(10.0, 20.0); // declara tambem a area

    printf("A area e: %f", area);

    return 0;
    }

    float areaRetang(float base, float altura){
        area = base * altura

    return area; // assim ele retornara pra dentro da funcao areaRetang
    }



